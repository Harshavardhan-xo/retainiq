# RetainIQ
### SaaS Customer Churn & Revenue-at-Risk Dashboard

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

The dashboard generates a reproducible synthetic SaaS dataset, trains a logistic-regression churn model, scores active customers, and exposes an interactive save list. Data is synthetic and the model is illustrative, not production-grade.